
<h2> Liste des episodes</h2>
<div>
    <?php 
    $book = $infos; 
    ?>        
    <li><a href= "<?php echo HOME ?>episode-edit/edit-<?php echo $book->getId(). '. . .' ?>">Ajouter un page/épisode</a></li>
    <?php
    foreach ($this->view_data as $table)
    { 
        if(!isset($episode))
        {
            $episode = $table; 
        } 
    } ?>
</div>
 
<!-- `quote``content``excerpt``createdDat``status``onlineDate``commented``nbComment``bookId``chapter``version``slugEps` -->
<div id="container" class="row">
    <table class="table-striped" >
        <thead>
            <tr>
                <th>Rf. Livre</th>
                <th>Rf. Volume</th>
                <th>Rf. Chapître</th>
                <th>quote</th>
                <th>extrait</th>
                <th>statut</th>
                <th>Der.commentaire</th>
                <th>voir +</th>
            </tr>
        </thead>
        <tbody>

            <?php 
            $table = '';
            foreach($this->view_data as $episode)
            {                                                                                                                                                                                                                        
                $table .= '<tr>';
                $table .= '<td>' . $episode->getBookId() . '</td>';
                $table .= '<td>' . $episode->getVolume() . '</td>';
                $table .= '<td>' . $episode->getChapter() . '</td>';
                $table .= '<td>' . $episode->getQuote() . '</td>';
                $table .= '<td>' . $episode->getExcerpt() . '</td>';
                $table .= '<td>' . $episode->getStatus() . '</td>';
                $table .= '<td>' . $episode->getCommented()  . '</td>';
  // var_dump('<td><a href= "\"' . HOME . 'episode-show/' . $episode->getBookId() .'-' . $episode->getId() . '>Visu</a></td>');die;

                $table .= '<td><a href='. HOME . 'episode-show/show-' . $episode->getBookId() . '.' . $episode->getVolume() . '.'. $episode->getId() . '>Visu</a></td>';
                $table .= '<td><a href='. HOME .'episode-edit/edit-' . $episode->getBookId() . '.' . $episode->getVolume() . '.'. $episode->getId() .'>Maj</a></td>';
                $table .= '<td><a href='. HOME. 'episode-edit/del-' . $episode->getId() . '>Del</a></td>';

                $table .= '<tr>';
            }
            print_r($table);
            
            ?>
        <tbody>
    </table>
</div>
